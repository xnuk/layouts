## How to flash
1. Open two file browser. One is a folder which has the firmware file (`*.uf2`).
2. Press `QK_BOOTLOADER` button (In this keymap: `Lower + Upper + T + Backspace`).
3. Mount "RPI-RP2" device, and open the folder.
4. Drag and drop `*.uf2` file into there.
5. Done!
